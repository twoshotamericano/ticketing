{
  "configurations": [
    {
      "name": "Docker Node.js Launch",
      "type": "docker",
      "request": "launch",
      "preLaunchTask": "docker-run: debug",
      "platform": "node",
      "node": {
        "package": "${workspaceFolder}/ticketing/auth/package.json",
        "localRoot": "${workspaceFolder}/ticketing/auth"
      }
    }
  ]
}