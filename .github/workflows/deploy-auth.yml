name: deploy-auth

on:
  push:
    branches:
      - master
    paths:
      - "ticketing/auth/**"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: cd ticketing/auth && docker build -t eanderson/auth .
